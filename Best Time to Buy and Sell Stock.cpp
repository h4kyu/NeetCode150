class Solution {
public:
    /*
    sol:
    int curr
    int fut
    int globalMin
    int globalMax
    int maxDiff
    iterate backwards
        if curr > globalMax
            globalMax = curr
        else if curr < globalMin
            globalMin = curr
        int currDiff = globalMax - globalMin
        if currDiff > maxDiff
            maxDiff = currDiff
    return maxDiff
    */
    int maxProfit(vector<int>& prices) {
        
    }
};
